# Use the official ClickHouse image
FROM bitnami/clickhouse:latest

# Switch to root to install packages if needed
USER root

# Install any additional tools or configurations
# Currently using the base bitnami image as-is, but this allows for future customization

# Switch back to the clickhouse user
USER 1001

# Expose the default ports
EXPOSE 8123 9000