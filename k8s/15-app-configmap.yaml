apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: better-bull-board
data:
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  REDIS_USE_TLS: "false"
  WEBSOCKET_URL: "ws://better-bull-board.local/ws"  # Update with your actual domain